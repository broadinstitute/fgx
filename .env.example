# Copy to .env and paste your GeneGenie API key.
# Generate at https://genegenie.broadinstitute.org/ -> MCP/API KEYS -> Create key
# The same key works for both the MCP and the REST API.
GENEGENIE_TOKEN=
